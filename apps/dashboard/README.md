# LogMind AI Dashboard

Next.js operations UI for projects, services, logs, incidents, AI analysis, and API keys.

```bash
npm run dev:dashboard
```

The dashboard runs at `http://localhost:3001` and proxies authenticated API calls to `LOGMIND_API_URL` using an HTTP-only session cookie.
